  Widget _buildCouncilCard() {
    final c = _councilData!;
    final urgencyColor = c.urgency.toLowerCase() == 'high'
        ? const Color(0xFFFF8B94)
        : c.urgency.toLowerCase() == 'medium'
            ? const Color(0xFFFFD3B6)
            : const Color(0xFFA8E6CF);

    // 4 fixed agent emojis matching the real API

    return Container(
      width: double.infinity,
      decoration: BoxDecoration(
        color: Colors.white,
        borderRadius: BorderRadius.circular(20),
        border: Border.all(color: Colors.black, width: 2.5),
        boxShadow: const [BoxShadow(color: Colors.black, offset: Offset(5, 5), blurRadius: 0)],
      ),
      child: ClipRRect(
        borderRadius: BorderRadius.circular(18),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            // â”€â”€ LAVENDER HEADER STRIPE â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
            Container(
              width: double.infinity,
              padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 12),
              color: const Color(0xFF7986CB), // Indigo/Lavender
              child: Row(
                children: [
                  const Text('ðŸ›ï¸', style: TextStyle(fontSize: 20)),
                  const SizedBox(width: 10),
                  Expanded(
                    child: Text('COUNCIL VERDICT',
                      style: GoogleFonts.poppins(
                        fontWeight: FontWeight.w900,
                        fontSize: 14,
                        color: Colors.white,
                        letterSpacing: 1.5,
                      )),
                  ),
                  // Urgency stamp
                  Container(
                    padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 4),
                    decoration: BoxDecoration(
                      color: urgencyColor,
                      borderRadius: BorderRadius.circular(6),
                      border: Border.all(color: Colors.white, width: 1.5),
                    ),
                    child: Text(c.urgency.toUpperCase(),
                      style: const TextStyle(fontWeight: FontWeight.w900, fontSize: 9, color: Colors.black)),
                  ),
                ],
              ),
            ),

            // â”€â”€ STATS TICKER STRIP â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
            Container(
              width: double.infinity,
              decoration: const BoxDecoration(
                color: Color(0xFFF0EDF9), // Very light lavender
                border: Border(bottom: BorderSide(color: Colors.black, width: 1.5)),
              ),
              child: Row(
                children: [
                  // LIVE pulse dot
                  Container(
                    padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 10),
                    decoration: const BoxDecoration(
                      border: Border(right: BorderSide(color: Color(0xFF9FA8DA), width: 1)),
                    ),
                    child: Row(
                      mainAxisSize: MainAxisSize.min,
                      children: [
                        Container(
                          width: 7, height: 7,
                          decoration: const BoxDecoration(
                            color: Color(0xFF43A047), // green
                            shape: BoxShape.circle,
                          ),
                        ),
                        const SizedBox(width: 5),
                        const Text('LIVE',
                          style: TextStyle(
                            fontWeight: FontWeight.w900, fontSize: 9,
                            color: Color(0xFF43A047), letterSpacing: 1.5,
                          )),
                      ],
                    ),
                  ),
                  // Stability block
                  Expanded(
                    child: Container(
                      padding: const EdgeInsets.symmetric(vertical: 10),
                      decoration: const BoxDecoration(
                        border: Border(right: BorderSide(color: Color(0xFF9FA8DA), width: 1)),
                      ),
                      child: Column(
                        children: [
                          Text(
                            '${_overviewData?.predictiveSummary?.stabilityScore ?? '--'}',
                            style: GoogleFonts.poppins(
                              fontWeight: FontWeight.w900, fontSize: 16, color: Color(0xFF5C6BC0)),
                          ),
                          const Text('STABILITY', style: TextStyle(fontSize: 7, color: Colors.black45, letterSpacing: 1, fontWeight: FontWeight.bold)),
                        ],
                      ),
                    ),
                  ),
                  // Risk block
                  Expanded(
                    child: Container(
                      padding: const EdgeInsets.symmetric(vertical: 10),
                      decoration: const BoxDecoration(
                        border: Border(right: BorderSide(color: Color(0xFF9FA8DA), width: 1)),
                      ),
                      child: Column(
                        children: [
                          Text(
                            '${_overviewData?.predictiveSummary?.failureProbability ?? '--'}%',
                            style: GoogleFonts.poppins(
                              fontWeight: FontWeight.w900, fontSize: 16,
                              color: (_overviewData?.predictiveSummary?.failureProbability ?? 0) > 40
                                  ? const Color(0xFFE53935) : const Color(0xFF2E7D32))),
                          const Text('RISK', style: TextStyle(fontSize: 7, color: Colors.black45, letterSpacing: 1, fontWeight: FontWeight.bold)),
                        ],
                      ),
                    ),
                  ),
                  // Agents block
                  Expanded(
                    child: Container(
                      padding: const EdgeInsets.symmetric(vertical: 10),
                      child: Column(
                        children: [
                          Text('4',
                            style: GoogleFonts.poppins(
                              fontWeight: FontWeight.w900, fontSize: 16, color: const Color(0xFFF9A825))), // amber
                          const Text('AGENTS', style: TextStyle(fontSize: 7, color: Colors.black45, letterSpacing: 1, fontWeight: FontWeight.bold)),
                        ],
                      ),
                    ),
                  ),
                ],
              ),
            ),

            // â”€â”€ BODY â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
            Padding(
              padding: const EdgeInsets.all(16),
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  // Focus area chip
                  Container(
                    padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 5),
                    decoration: BoxDecoration(
                      color: const Color(0xFFC5CAE9),
                      borderRadius: BorderRadius.circular(8),
                      border: Border.all(color: Colors.black, width: 1.5),
                    ),
                    child: Row(
                      mainAxisSize: MainAxisSize.min,
                      children: [
                        const Text('ðŸŽ¯', style: TextStyle(fontSize: 12)),
                        const SizedBox(width: 6),
                        Text('FOCUS: ${c.priorityFocusArea.toUpperCase()}',
                          style: const TextStyle(fontWeight: FontWeight.w900, fontSize: 10, letterSpacing: 0.5)),
                      ],
                    ),
                  ),

                  // Risk sentence callout
                  if (c.riskSentence.isNotEmpty) ...[
                    const SizedBox(height: 12),
                    Container(
                      width: double.infinity,
                      padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 8),
                      decoration: BoxDecoration(
                        color: urgencyColor.withOpacity(0.3),
                        borderRadius: BorderRadius.circular(10),
                        border: Border.all(color: Colors.black, width: 1.5),
                      ),
                      child: Text(c.riskSentence,
                        style: const TextStyle(fontWeight: FontWeight.w900, fontSize: 12)),
                    ),
                  ],

                  // Summary
                  const SizedBox(height: 12),
                  Text(c.summary,
                    style: const TextStyle(fontWeight: FontWeight.w500, fontSize: 12, color: Colors.black87, height: 1.5)),

                  // Recommended Action â€” bold bottom strip
                  if (c.recommendedAction.isNotEmpty) ...[
                    const SizedBox(height: 14),
                    Row(
                      crossAxisAlignment: CrossAxisAlignment.start,
                      children: [
                        Container(
                          width: 4, height: 48,
                          decoration: BoxDecoration(
                            color: urgencyColor,
                            borderRadius: BorderRadius.circular(4),
                            border: Border.all(color: Colors.black, width: 1),
                          ),
                        ),
                        const SizedBox(width: 10),
                        Expanded(
                          child: Column(
                            crossAxisAlignment: CrossAxisAlignment.start,
                            children: [
                              const Text('RECOMMENDED ACTION',
                                style: TextStyle(fontWeight: FontWeight.w900, fontSize: 9, letterSpacing: 1, color: Colors.black54)),
                              const SizedBox(height: 3),
                              Text(c.recommendedAction,
                                style: const TextStyle(fontWeight: FontWeight.bold, fontSize: 11, height: 1.4)),
                            ],
                          ),
                        ),
                      ],
                    ),
                  ],
                ],
              ),
            ),
          ],
        ),
      ),
    );
